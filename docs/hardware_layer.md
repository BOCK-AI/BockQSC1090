# Hardware Layer

The `main_10qubit_design.py` script generates the GDSII layout for a 10-qubit quantum processor using `gdstk`.
It includes:
- Transmon qubits in a 2x5 grid.
- Nearest-neighbor couplers.
- Readout resonators.
