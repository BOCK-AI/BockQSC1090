# SHA-256 Module

The `sha256/` directory contains a quantum-inspired circuit representation of SHA-256 compression functions. This serves as a stress-test for the compilation and scheduling pipeline.
