# Configuration

Configuration files are located in `config/`:
- `qubit_params.json`: Physical qubit parameters (frequencies, anharmonicity).
- `system_config.json`: System-wide settings.
- `calibration_schedule.json`: Schedule for calibration tasks.
