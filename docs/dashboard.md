# Dashboard

The Streamlit dashboard (`dashboard.py`) provides a visual overview of the QPU state, recent benchmarks, and an interface to run `.algo` files interactively.

Run with: `streamlit run dashboard.py`
