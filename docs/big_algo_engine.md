# Big Algorithm Engine

The Big Algorithm Engine (`big_algo_engine/`) provides a DSL (`.algo`) for running complex circuits.
Features:
- **Parser**: Reads `.algo` files.
- **Compiler**: Translates to intermediate gates.
- **Scheduler**: Groups gates into execution slots.
